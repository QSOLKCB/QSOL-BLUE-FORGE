# v1 Synthetic Fixtures

Fixtures in this directory are inert structured data used to test the
deterministic BLUE-FORGE reference core.

They are not executed.

`path-traversal-case.json` models an authorized synthetic hardening case using
hash-bound evidence identities. The fixture demonstrates attack-class
generalization plus benign controls without embedding executable code.
